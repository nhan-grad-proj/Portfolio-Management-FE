export type AddPortfolioModalModel = {
  name: string;
  description: string;
};

export type PortfolioOverviewColumn = {
  name: string;
  description: string;
  createdAt: string;
  action: number;
};
